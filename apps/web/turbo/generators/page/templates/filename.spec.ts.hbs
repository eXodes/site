import { expect, test } from "@playwright/test";

test("{{ pascalCase name }} page has expected title", async ({ page }) => {
  await page.goto("{{ route }}");

  expect(await page.title()).toBe("{{ pascalCase name }}");
});
